<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0"
         xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
         xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/xsd/maven-4.0.0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <groupId>com.gamma.agentkernel</groupId>
  <artifactId>agent-kernel-parent</artifactId>
  <version>0.1.0-SNAPSHOT</version>
  <packaging>pom</packaging>

  <name>agent-kernel (parent)</name>
  <description>
    Reusable, framework-agnostic agent core shared by UCC File-Processor assist, CVVE V and V-as-a-Service,
    and the Real-Estate CxO Decision-Support agent. Ring-1 core has zero runtime dependencies.
    See docs/ARCHITECTURE.md and docs/adr/.
  </description>

  <modules>
    <module>agent-kernel-core</module>
    <module>agent-orchestration</module>
    <module>agent-provider-ollama</module>
    <module>agent-eval</module>
  </modules>

  <properties>
    <maven.compiler.release>25</maven.compiler.release>
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding>
    <project.reporting.outputEncoding>UTF-8</project.reporting.outputEncoding>

    <!-- Dependency versions (pinned here; modules reference without versions). -->
    <junit.version>5.10.2</junit.version>
    <langchain4j.version>1.15.1</langchain4j.version>
    <jackson.version>2.18.3</jackson.version>

    <!-- Plugin versions. -->
    <compiler.plugin.version>3.13.0</compiler.plugin.version>
    <surefire.plugin.version>3.2.5</surefire.plugin.version>
    <jar.plugin.version>3.4.1</jar.plugin.version>
    <enforcer.plugin.version>3.5.0</enforcer.plugin.version>
  </properties>

  <dependencyManagement>
    <dependencies>
      <!-- Reactor modules (so consumers and sibling modules reference without versions). -->
      <dependency>
        <groupId>com.gamma.agentkernel</groupId>
        <artifactId>agent-kernel-core</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.gamma.agentkernel</groupId>
        <artifactId>agent-orchestration</artifactId>
        <version>${project.version}</version>
      </dependency>
      <dependency>
        <groupId>com.gamma.agentkernel</groupId>
        <artifactId>agent-eval</artifactId>
        <version>${project.version}</version>
      </dependency>

      <!-- Test (all modules). -->
      <dependency>
        <groupId>org.junit</groupId>
        <artifactId>junit-bom</artifactId>
        <version>${junit.version}</version>
        <type>pom</type>
        <scope>import</scope>
      </dependency>

      <!-- Ring-2 runtime deps, pinned for K1 to consume (NOT pulled into ring-1 core). -->
      <dependency>
        <groupId>dev.langchain4j</groupId>
        <artifactId>langchain4j-ollama</artifactId>
        <version>${langchain4j.version}</version>
      </dependency>
      <dependency>
        <groupId>com.fasterxml.jackson.core</groupId>
        <artifactId>jackson-databind</artifactId>
        <version>${jackson.version}</version>
      </dependency>
    </dependencies>
  </dependencyManagement>

  <build>
    <pluginManagement>
      <plugins>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-compiler-plugin</artifactId>
          <version>${compiler.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-surefire-plugin</artifactId>
          <version>${surefire.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-jar-plugin</artifactId>
          <version>${jar.plugin.version}</version>
        </plugin>
        <plugin>
          <groupId>org.apache.maven.plugins</groupId>
          <artifactId>maven-enforcer-plugin</artifactId>
          <version>${enforcer.plugin.version}</version>
        </plugin>
      </plugins>
    </pluginManagement>

    <plugins>
      <!-- Basic build hygiene across all modules. -->
      <plugin>
        <groupId>org.apache.maven.plugins</groupId>
        <artifactId>maven-enforcer-plugin</artifactId>
        <executions>
          <execution>
            <id>enforce-build-environment</id>
            <goals><goal>enforce</goal></goals>
            <configuration>
              <rules>
                <requireMavenVersion>
                  <version>[3.9,)</version>
                </requireMavenVersion>
              </rules>
            </configuration>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>

  <distributionManagement>
    <repository>
      <id>github</id>
      <name>GitHub Packages</name>
      <url>https://maven.pkg.github.com/jotder/agent-kernel</url>
    </repository>
    <snapshotRepository>
      <id>github</id>
      <name>GitHub Packages</name>
      <url>https://maven.pkg.github.com/jotder/agent-kernel</url>
    </snapshotRepository>
  </distributionManagement>

</project>
